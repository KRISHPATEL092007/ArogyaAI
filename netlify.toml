[build]
command = "npm run build"
publish = "dist/spa"

# The backend is now a standalone Python (Flask) app under /backend — it is
# not deployed as a Netlify Function. Deploy it separately (Render, Railway,
# a VM, etc.) and point VITE_API_BASE_URL (or your proxy/host config) at it.
